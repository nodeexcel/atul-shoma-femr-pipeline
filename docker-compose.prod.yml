services:
  web:
    build:
      context: .
      target: production
    ports:
      - "8000:8000"
    env_file: .env.prod
    volumes:
      - media_prod:/app/media
      - logs_prod:/app/logs
    restart: unless-stopped

volumes:
  media_prod:
  logs_prod:
